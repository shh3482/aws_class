package day07;

public class EX07_Network {

	public static void main(String[] args) {
		// TODO Auto-generated method stub
		/* TCP
		 * - 데이터 전송 속도가 느림
		 * - 안절적으로 통신
		 * - 연결 지향적
		 * 
		 * UDP
		 * - 데이터 전속 속도가 빠름
		 * - 신뢰성이 없음
		 * - 비연결 지향적
		 * 
		 * 소케 프로그래밍
		 * - 소켁을 이용한 통신 프로그램
		 * - 들어올수 있는 두멍을 만들고, 그 구명을 통해 통신
		 * 
		 *  소켓 프로그래밍
		 *  - 서버
		 *  1. 서버의 포트를 지정
		 *  2. 서버용 소켓 객체를 생성
		 *  3. 접속 대기
		 *  4. 접속 요청이 오면 수락 후 클라이언트 소켈 객첼를 생성
		 *  5. 연결된 소켓 객체에서 10 스트림을 이용하여 읽고 쓰기를 함.
		 *  6. 통신 종료
		 *  
		 * 클라이언트
		 * 1. 서버의 IP주소와 포트를 이용하여 클라이언트 객체를 생성
		 * 2. 연결 요청
		 * 3. 연결이 되면 소켓 객체에서 IO스트림을 이용하여 읽고 쓰기를 함
		 * 4. 통신을 종료
		 *  
		 */
	}

}
